#include "dog.h"
#include <stdio.h>
/**
 * print_dog-function that prints a struct dog
 * @d: variabl struct
 * Return: nhothing
 */
void print_dog(struct dog *d)
{
	if (d is NULL)
	{
		printf("");
	}
	else if (name is NULL)
	{
		printf("name: (nil)");
	}
}
