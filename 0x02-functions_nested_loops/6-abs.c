#include "main.h"
/**
 * _abs-print the absolute value of integer
 * @x:checker
 * Return:the absolute value;
 */
int _abs(int x)
{
	int x;

	if (x >= 0)
	{
		return (x);
	}
	else
	{
		return (-x);
	}
}
